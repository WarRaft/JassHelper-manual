# assignment

An assignment inside a function/method simply sets the value inside a variable, or array. It is syntax is
`(thing to assign) = (value)`. You may see many examples of assignments in the snippets above and bellow.